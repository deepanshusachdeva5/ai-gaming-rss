# Copy this to .env and fill in your token.
# GitHub PAT raises API limit from 60 → 5,000 requests/hour.
# Create one at: https://github.com/settings/tokens
# Only needs the default (public) scope — no special permissions required.
GITHUB_TOKEN=your_token_here
