# Copy this to .env and fill in your tokens.

# GitHub PAT raises API limit from 60 → 5,000 requests/hour.
# Create one at: https://github.com/settings/tokens
# Only needs the default (public) scope — no special permissions required.
GITHUB_TOKEN=your_github_token_here

# Tavily API key — required to scrape websites without RSS feeds.
# Get a free key (1,000 searches/month) at: https://app.tavily.com
TAVILY_API_KEY=tvly-your_key_here
